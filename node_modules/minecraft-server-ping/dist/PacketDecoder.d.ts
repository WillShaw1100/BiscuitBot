/// <reference types="node" />
import internal, { Writable } from 'stream';
export declare class PacketDecoder extends Writable {
    private packetInfo;
    private buffer;
    constructor(options?: internal.WritableOptions | undefined);
    oncePromise<T extends unknown>(event: string): Promise<T>;
    _write(chunk: Buffer, encoding: any, callback: () => void): Promise<void>;
    private decodeHeader;
    private getPayload;
    private decodeHandshake;
    private decodePong;
}
